#include<bits/stdc++.h>

using namespace std;

int main(){
/*     ofstream outfile("my.txt");
    outfile<<"hello"<<endl;
    outfile<<23<<endl;
    outfile.close(); */
  /*   ifstream ifile;
    ifile.open("my.txt");
    if(!ifile.is_open()){
        cout<<"file not open";
    }
    else{
        string hello;
        int x;
        ifile>>hello;
        ifile>>x;
        cout<<hello<<x;
    } */

}